{
  "name": "civitas-demo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "build": "cd frontend && npm install --include=dev && VITE_MAPBOX_ACCESS_TOKEN=$VITE_MAPBOX_ACCESS_TOKEN npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
